<div class="product-index">
  <h1>Love Phoebe</h1>

  <div class="image-products">
    <ul class="list-products">
      <% @products.each do |p| %>
        <%= link_to image_tag(p.image2), product_path(p.id), :class=> 'products_index' %>
      <%end%>
    </ul>
  </div>
</div>